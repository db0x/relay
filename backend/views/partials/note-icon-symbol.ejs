<%# note.svg als <symbol>, damit die Notiz-Icons per <use> eingebunden und
    einzeln eingefaerbt werden koennen (als <img> ginge das nicht: dessen
    Inhalt ist ein eigenes Dokument, das CSS des Hosts erreicht es nicht).
    Das Markup ERZEUGT noteicon.js aus der Datei — von Hand gepflegt driftete
    es von note.svg weg, und Aenderungen an der Zeichnung kamen nur an den
    <img>-Stellen an. --note-body/--note-fold erben in den Schatten-Baum des
    <use> hinein; ohne gesetzte Werte greifen die Farben der Originaldatei. %>
<%- noteSymbol %>
